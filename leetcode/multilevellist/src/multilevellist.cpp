#include <bits/stdc++.h>

using namespace std; 

struct DataNode{
    int val; 
    DataNode* next; 
    DataNode(int x) {val=x;}
};

struct ListNode {
      int val;
      DataNode* rowHead; 
      ListNode* nextRow;
      ListNode(int x) { val = x; }
};

int main(){


}